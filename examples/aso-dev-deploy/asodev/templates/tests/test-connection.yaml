apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "asodev.fullname" . }}-test-connection"
  labels:
    {{- include "asodev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "asodev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
